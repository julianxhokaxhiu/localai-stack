services:
  lemonade:
    command:
      - sh
      - -lc
      - |
        if [ ! -f /opt/lemonade/.cache/lemonade/config.json ]; then
          cp /opt/lemonade/.cache/lemonade/.seed-config.json /opt/lemonade/.cache/lemonade/config.json
        fi
        exec ./lemond --host 0.0.0.0
    volumes:
      - ./lemonade-configs/cpu.json:/opt/lemonade/.cache/lemonade/.seed-config.json:ro

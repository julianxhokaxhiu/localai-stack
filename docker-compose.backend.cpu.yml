services:
  lemonade:
    command:
      - sh
      - -lc
      - |
        if [ ! -f /root/.cache/lemonade/config.json ]; then
          cp /root/.cache/lemonade/.seed-config.json /root/.cache/lemonade/config.json
        fi
        exec ./lemond --host 0.0.0.0
    volumes:
      - ./lemonade-configs/cpu.json:/root/.cache/lemonade/.seed-config.json:ro

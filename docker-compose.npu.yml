services:
  lemonade:
    volumes:
      # Persist fastflow models
      - lemonade-flm:/opt/lemonade/.config/flm/models
    devices:
      - /dev/accel/accel0:/dev/accel/accel0

volumes:
  lemonade-flm:
